# Learning Record 0044 — Audit a Verified Next Instruction

## Date
2026-09-02

## Lesson
0044 — Audit a Verified Next Instruction

## What was taught

- The learner compares the exact Lesson 43 instruction with each of its two covered attempts before interpreting outcomes.
- Each attempt receives one evidence label: `followed`, `not followed`, or `unclear`.
- If either attempt is `not followed` or `unclear`, the learner repeats the same instruction for two ordinary attempts and does not interpret cue/start outcomes from that pair.
- Only two `followed` labels create a verified pair ready for the existing cue-then-start triage.

## Key insight

A pair can answer a plan only after the plan was enacted: **read the exact instruction first; if either attempt is unverified, repeat it calmly before reading outcomes.**

## Zone of proximal development notes

The learner has written a verified next instruction, but actual records remain unknown. This lesson adds an immediate fidelity gate before reusing the familiar cue/start triage. Preserve the two-minute baseline, chosen frequency, no-penalty stance, and one-variable approach. Do not add duration, catch-up work, multiple changes, or a larger tracking system.

## What comes next

After a verified pair, teach a compact cue/start classification refresh: label each attempt `cue`, `start-side`, `none`, or `unclear`, then retain the plan unless the same readable earliest break appears twice and supports one already-specified matching test.
