# Learning Record 0056 — Audit a Verified-Pair Instruction

## Date
2026-09-14

## Lesson
0056 — Audit a Verified-Pair Instruction

## What was taught

- The learner compares the exact Lesson 55 next-instruction sentence with each of its two covered ordinary attempts before interpreting outcomes.
- Each attempt receives one evidence label: `followed`, `not followed`, or `unclear`; missing evidence is not reconstructed from memory.
- If either label is `not followed` or `unclear`, the learner repeats the exact instruction for two ordinary attempts without changing a link or interpreting cue/start outcomes.
- Only two `followed` labels create a verified pair ready for the existing cue-then-start classification.

## Key insight

A pair can answer a plan only after the plan was enacted: **audit the exact sentence first; if either attempt is unverified, repeat it calmly before reading outcomes.**

## Zone of proximal development notes

The learner can write a verified-pair instruction and has practised this audit structure in earlier cycles, but actual records remain unknown. This lesson applies the fidelity gate to the newly written instruction without increasing duration, frequency, tracking burden, or output expectations. Preserve the two-minute baseline, chosen frequency, no-penalty stance, and one-variable boundary. Do not add catch-up work, multiple changes, or a larger tracking system.

## What comes next

After both covered attempts are labelled `followed`, teach a compact verified-pair classification: read `cue occurred?` then `two-minute action started?`, label each `cue`, `start-side`, `none`, or `unclear`, and retain unless the same readable earliest break appears twice and supports one already-specified matching test.
