# Learning Record 0047 — Audit an Audited-Pair Instruction

## Date
2026-09-05

## Lesson
0047 — Audit an Audited-Pair Instruction

## What was taught

- The learner compares the exact Lesson 46 instruction with each of its two covered ordinary attempts before interpreting outcomes.
- Each attempt receives one evidence label: `followed`, `not followed`, or `unclear`; gaps are not reconstructed from memory.
- If either label is `not followed` or `unclear`, the learner repeats the exact instruction for two ordinary attempts and does not yet interpret cue/start outcomes.
- Only two `followed` labels create a verified pair ready for the existing cue-then-start triage.

## Key insight

A pair can answer the plan only after the plan was enacted: **read the exact instruction, label both attempts, then either repeat it for readable evidence or read a verified pair.**

## Zone of proximal development notes

The learner can write an audited-pair instruction and has previously practiced this audit pattern, but actual records remain unknown. This lesson restores the immediate fidelity gate without adding a system component. Preserve the two-minute baseline, chosen frequency, no-penalty stance, and one-variable boundary. Do not add duration, catch-up work, multiple changes, or more tracking.

## What comes next

After both covered attempts are labelled `followed`, teach a compact cue/start classification of the verified pair: label each `cue`, `start-side`, `none`, or `unclear`, then retain unless the same readable earliest break appears twice and supports one already-specified matching test.
